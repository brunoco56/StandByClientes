@model IEnumerable<StandByClientes.Entidades.Cliente>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Criar Novo</a>
<p />
@*<p>
        @using (Html.BeginForm("Index", "Clientes", FormMethod.Get))
        {
            <label>@Html.RadioButton("Ativo", 1)Ativo</label>;
            <label>@Html.RadioButton("Ativo", -1)Inativo</label>;
            <label> @Html.TextBox("razaoSocial") Razão Social </label>
            <label> @Html.TextBox("cnpj") CNPJ </label>
            <input type="submit" value="Pesquisar" id="Pesquisar" />;
        }
    </p>*@
<div class="row">

    <form class="form-inline">

        <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" name="Ativo" id="Ativo" value="1" checked>
            <label class="form-check-label" for="inlineRadio1">Ativo</label>
        </div>
        <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" name="Ativo" id="Ativo" value="-1" >
            <label class="form-check-label" for="inlineRadio2">Inativo</label>
        </div>


        <div class="form-group mx-sm-3 mb-2">
            <label for="inputPassword2" class="sr-only">Razão Social</label>
            <input type="text" class="form-control" id="razaosocial" placeholder="RazaoSocial">
        </div>
        <div class="form-group mx-sm-3 mb-2">
            <label for="inputPassword2" class="sr-only">Cnpj</label>
            <input type="text" class="form-control" id="cnpj" placeholder="Cnpj">
        </div>
        <button type="submit" class="btn btn-primary mb-2" id="Pesquisar">Pesquisar</button>

    </form>

</div>

@Html.Partial("_Clientes")

<script type="text/javascript" src="~/js/Clientes/Index.js"></script>
